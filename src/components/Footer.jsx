const Footer = () => {
    return (
            <footer className="text-center py-2 md:py-2 mt-auto bg-black backdrop-blur-sm">
        <p className="text-xs text-gray-500">© {new Date().getFullYear()} Adam Holm. All rights reserved.</p>
    </footer>
    );
};

export default Footer;
